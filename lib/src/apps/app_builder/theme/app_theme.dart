// App Builder uses the Studio theme (Material 3 light/dark + the shared
// `vibeMono` text style). The fork is gone; this re-exports the host's
// single theme so App Builder looks identical to the rest of the Studio.
export 'package:appplayer_studio/base.dart' show AppTheme, vibeMono;
