// App Builder uses the platform's properties panel — the 12-layer
// per-field editor (mode-aware card numbering via the optional
// `modeLayers` param, now in base). The fork (an identical 7k-line copy
// that differed only by that one param) is gone; this re-exports it.
export '../../../base/widgets/properties_panel.dart';
