// App Builder uses the platform's edit-history viewer — the Studio
// lifecycle's `showHistoryDialog` renders the canonical patch timeline
// (undo/redo audit) from the history log. The fork is gone; re-exported.
export '../../../base/widgets/history_dialog.dart';
