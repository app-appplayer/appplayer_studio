type: VbuBusyIndicator
category: atom
source: custom
description: |
  Vbu atom — BusyIndicator. Schema auto-extracted from factory; refine description and examples in follow-up.
properties:
  label:
    type: "string"
    description: "(auto) resolve"
  dotColor:
    type: "string"
    description: "(auto) color"
  cycleMs:
    type: "number"
    description: "(auto) resolve"
examples: []
