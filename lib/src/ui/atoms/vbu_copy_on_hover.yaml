type: VbuCopyOnHover
category: utility
source: custom
description: |
  Vbu atom — CopyOnHover. Schema auto-extracted from factory; refine description and examples in follow-up.
properties:
  text:
    type: "string"
    description: "(auto) resolve"
  copiedSnackText:
    type: "string"
    description: "(auto) resolve"
  child:
    type: "Widget"
    description: "(auto) child"
  onDelete:
    type: "Action"
    description: "(auto) callback"
examples: []
